import {
  init,
  setUser,
  recordBreadcrumb,
  addCustomData,
  addTag,
  updateCustomData,
  clearSession,
  sendRUMTimingEvent,
  sendError
} from './RaygunClient';

export default {
  init,
  setUser,
  recordBreadcrumb,
  addCustomData,
  addTag,
  updateCustomData,
  clearSession,
  sendRUMTimingEvent,
  sendError
};
