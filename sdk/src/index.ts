import {
  init,
  addTag,
  setUser,
  clearSession,
  recordBreadcrumb,
  addCustomData,
  sendError,
  updateCustomData,
  sendRUMTimingEvent
} from './RaygunClient';

export default {
  init,
  addTag,
  setUser,
  clearSession,
  recordBreadcrumb,
  addCustomData,
  sendError,
  updateCustomData,
  sendRUMTimingEvent
};
